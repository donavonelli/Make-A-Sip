<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Ingredients</title>
</head>
<body>
    <h1>View List of Ingredients</h1>
    <ul>
        <% ingredients.forEach(ingredient => { %>
        <li>
            <a href="/recipes/<%= ingredient._id %>"> <%= ingredient.name %> </a>
        </li>
        <% }) %>
    </ul>

    <% if(ingredients.length === 0) { %>
    <p>There are no ingredients available yet.</p>
    <% } %>
</body>
</html>